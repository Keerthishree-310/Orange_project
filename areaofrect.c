#include<stdio.h>
int main()
{
    int l,b;
    printf("Enter the length and breath of the rectangle:");
    scanf("%d %d",&l,&b);
    int area=l*b;
    printf("The area of the rectangle=%d sq.unit",area);
    return 0;
}
